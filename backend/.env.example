PORT=3001
ADMIN_PASSWORD=your-secret-admin-password
READER_TOKEN=your-reader-archive-token
FRONTEND_URL=http://localhost:3000

# Production: PostgreSQL connection string
# Local dev (Render DB): dùng External Database URL + ?sslmode=require
# Deploy backend trên Render: dùng Internal Database URL (nhanh hơn, không qua internet)
# DATABASE_URL=postgresql://user:pass@dpg-xxx-a.oregon-postgres.render.com/dbname?sslmode=require
